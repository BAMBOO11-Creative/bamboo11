{{head-layout}}

{{#if contentservice.modal}}

    {{outlet}}

{{else}}

    {{bm-header}}

    {{home/hm-home open='openModal'}}

    {{#each content.slides as |slide|}}

        {{#if slide.component}}

            {{component slide.component model=slide}}

        {{/if}}

    {{/each}}

{{/if}}
