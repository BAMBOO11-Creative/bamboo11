<div class="logo">
    <h1>{{content.html.title}}</h1>
    <img src="assets/images/bamboo11-logo.svg">
</div>

<nav>

    <ul>

        {{#each content.navigation as |item|}}

            <li {{action 'navigate' item}} active={{item.active}}>
                <span>{{item.title}}</span>
                <span></span>
            </li>

        {{/each}}

    </ul>

</nav>
